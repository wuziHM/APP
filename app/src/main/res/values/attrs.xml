<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="Gallery">
        <attr name="android:galleryItemBackground" />
    </declare-styleable>

    <!--下拉刷新的部分-->
    <declare-styleable name="PtrFrameLayout">
        <!-- Optional. If you put header and content in xml, you can you these to specify them. -->
        <attr name="ptr_header" format="reference" />
        <attr name="ptr_content" format="reference" />

        <!-- the resistance when you are moving the frame -->
        <attr name="ptr_resistance" format="float" />

        <!-- the ration of the height of the header to trigger refresh -->
        <attr name="ptr_ratio_of_header_height_to_refresh" format="float" />

        <attr name="ptr_duration_to_close" format="integer" />
        <attr name="ptr_duration_to_close_header" format="integer" />

        <!-- pull to refresh, otherwise release to refresh, default is release to refresh -->
        <attr name="ptr_pull_to_fresh" format="boolean" />

        <!-- keep header when refreshing-->
        <attr name="ptr_keep_header_when_refresh" format="boolean" />
    </declare-styleable>

    <declare-styleable name="PtrClassicHeader">
        <attr name="ptr_rotate_ani_time" format="integer" />
    </declare-styleable>

    <declare-styleable name="MyFirstView">
        <attr name="backColor" format="color|reference" />
        <attr name="mTextColor" format="color|reference" />
        <attr name="mText" format="string|reference" />
    </declare-styleable>

    <declare-styleable name="MySecondView">
        <attr name="secText" format="reference|string" />
        <attr name="secTextSize" format="reference|dimension" />
        <attr name="secTextColor" format="reference|color" />
        <attr name="secImage" format="reference" />
        <attr name="secImageType">
            <enum name="fitXY" value="0" />
            <enum name="center" value="1" />
        </attr>
    </declare-styleable>
    <declare-styleable name="MyThirdView">
        <attr name="thiBeforeColor" format="color|reference" />
        <attr name="thiLaterColor" format="color|reference" />
        <attr name="thiCircleWidth" format="dimension" />
        <attr name="thiSpeed" format="integer" />
    </declare-styleable>

    <declare-styleable name="MyFourthView">
        <attr name="firstColor" format="color|reference" />
        <attr name="secondColor" format="color|reference" />
        <attr name="circleWidth" format="dimension|reference" />
        <attr name="dotCount" format="integer" />
        <attr name="splitSize" format="integer" />
        <attr name="bg" format="reference" />
    </declare-styleable>


    <declare-styleable name="HorizontalBar">
        <attr name="oneStart" format="integer" />
        <attr name="twoStart" format="integer" />
        <attr name="threeStart" format="integer" />
        <attr name="fourStart" format="integer" />
        <attr name="fiveStart" format="integer" />
        <attr name="horHeight" format="dimension|reference" />
        <attr name="showCount" format="boolean" />
        <attr name="showStar" format="boolean" />
    </declare-styleable>
</resources>